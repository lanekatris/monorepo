---
interface Props {
	date?: Date
}

const { date } = Astro.props
console.log(date)
---

<time>
	{
		// date.toLocaleDateString('en-us', {
		// 	year: 'numeric',
		// 	month: 'short',
		// 	day: 'numeric',
		// })

		date ? date.toISOString().split('T')[0] : null
	}
</time>
